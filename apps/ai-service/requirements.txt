fastapi==0.115.5
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12
# Speech recognition stack — re-use the FunASR install from the fine-tune env
# (use the in-project vendored source with `pip install -e ../../../FunASR` if
# you prefer editable; otherwise the published package works too).
funasr>=1.2.6
torch>=2.1
torchaudio>=2.1
numpy>=1.23
soundfile>=0.12
pypinyin>=0.49
# Tiktoken is needed by some FunASR models; harmless if unused.
tiktoken>=0.7
